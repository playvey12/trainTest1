<!DOCTYPE html>
<html lang="ru">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="./css/login.css" />
    {{> seo}}
    <title>{{t.reg.register_title}}</title>
    <script src="https://telegram.org/js/telegram-web-app.js"></script>

</head>

<body>
    <div class="container">
        <header class="hero-section">
            <h1 class="main-title">{{t.reg.hero_title}}</h1>
            <p class="subtitle">{{t.reg.hero_subtitle}}</p>
        </header>

        <main class="main-content">
            <div class="registration-card">
                <div class="card-header">
                    <span class="step-text">{{t.auth.step_label}}</span>
                    <span class="step-count">{{t.auth.step_1_of_3}}</span>
                </div>
                <div class="progress-bar">
                    <div class="progress-fill" style="width: 33%;"></div>
                </div>

                <form id="registrationForm">
                    <div class="form-group">
                        <label class="form-label">{{t.auth.email_label}}</label>
                        <div class="input-wrapper">
                            <span class="input-icon"><i class="fa-regular fa-envelope"></i></span>
                            <input type="email" class="form-input" id="email" placeholder="{{t.auth.email_placeholder}}"
                                required>
                        </div>
                        <div class="error-message" id="emailError" style="display: none;">{{t.auth.email_error}}</div>
                    </div>

                    <div class="form-group">
                        <label class="form-label">{{t.auth.password_label}}</label>
                        <div class="password-wrapper">
                            <span class="input-icon"><i class="fa-solid fa-lock"></i></span>
                            <input type="password" class="form-input" id="password" placeholder="••••••••••••" required>
                            <button type="button" class="toggle-password" id="togglePassword">
                                <i class="fa-regular fa-eye"></i>
                            </button>
                        </div>
                        <div class="error-message" id="passwordError" style="display: none;">Пароль слишком короткий
                            (мин. 8 симв.)</div>
                    </div>

                    <div class="form-group">
                        <label class="form-label">{{t.reg.confirm_password_label}}</label>

                        <div class="password-wrapper">
                            <span class="input-icon"><i class="fa-solid fa-lock"></i></span>
                            <input type="password" class="form-input" id="confirmPassword" placeholder="••••••••••••"
                                required>
                            <button type="button" class="toggle-password" id="toggleConfirmPassword">
                                <i class="fa-regular fa-eye"></i>
                            </button>
                        </div>

                        <div class="privacy-checkbox" id="privacyCheckbox">
                            <input type="checkbox" id="privacyAgreement" required>
                            <label for="privacyAgreement" class="checkbox-label">
                                {{t.reg.agreement_text}}
                                <a href="#" id="showPrivacyPolicy" class="privacy-link">{{t.reg.policy_link}}</a>
                            </label>
                        </div>

                        <button type="submit" class="submit-btn" onclick="registerUser(event)" id="submitBtn">
                            <span id="btnText">{{t.reg.register_btn}}</span>
                        </button>

                        <div class="divider">
                            <span>{{t.auth.social_divider}}</span>
                        </div>

                        <div class="auth-footer">
                            <div class="footer-row">
                                <div class="login-link">
                                    {{t.reg.already_have_acc_login}}
                                    <a href="/login" class="sign-in-link ">{{t.reg.login_link_text}}</a>

                                </div>
                            </div>
                        </div>
                </form>
            </div>
        </main>
        {{>privacyModal}}
        <footer class="footer-info">
            {{t.auth.footer_terms}}
        </footer>
    </div>
    <div class="modal-overlay" id="confirmationModal">
        <div class="modal">
            <div class="modal-header">
                <h2 class="modal-title">Подтверждение email</h2>
                <button class="close-btn" id="closeModal">&times;</button>
            </div>
            <div class="modal-body">
                <p class="modal-text">Мы отправили 6-значный код подтверждения на адрес <span id="userEmail"></span>.
                    Пожалуйста, введите его ниже:</p>

                <div class="code-input-container">
                    <div class="code-inputs">
                        <input type="text" class="code-input" maxlength="1" data-index="0" autocomplete="off">
                        <input type="text" class="code-input" maxlength="1" data-index="1" autocomplete="off">
                        <input type="text" class="code-input" maxlength="1" data-index="2" autocomplete="off">
                        <input type="text" class="code-input" maxlength="1" data-index="3" autocomplete="off">
                        <input type="text" class="code-input" maxlength="1" data-index="4" autocomplete="off">
                        <input type="text" class="code-input" maxlength="1" data-index="5" autocomplete="off">
                    </div>
                    <div class="error-message" id="codeError" style="display: none;">Неверный код подтверждения
                    </div>
                </div>

                <div class="modal-footer">
                    <button class="resend-btn" id="resendCode" disabled>Отправить код повторно</button>
                    <div class="timer" id="timer" style="display: none;">Отправить повторно через: <span
                            id="countdown">60</span> сек.</div>
                    <button class="submit-btn" id="confirmCodeBtn" disabled>
                        <span id="confirmBtnText">Подтвердить</span>
                        <div class="loader" id="confirmLoader" style="display: none;"></div>
                    </button>
                </div>
            </div>
        </div>
    </div>
    {{>confirmationModal}}
    <script src="./js/auth.js"></script>
    <script src="./js/register.js"></script>


</body>

</html>