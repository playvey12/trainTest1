<div id="editExerciseModal" class="modal-overlay">
  <div class="modal">
    <div class="modal-header">
      <h2 class="modal-title">Редактировать упражнение</h2>
      <button class="close-btn" onclick="closeEditModal()">×</button>
    </div>

    <div class="modal-body">
      <form id="editExerciseForm">
        <div class="form-group">
          <label class="form-label" for="edit-exercise-name">Название упражнения</label>
          <input
            type="text"
            id="edit-exercise-name"
            class="form-input"
            placeholder="Введите название упражнения"
            required
          />
        </div>

        <div class="form-group">
          <label class="form-label" for="edit-exercise-weight">Вес (кг)</label>
          <input
            type="number"
            id="edit-exercise-weight"
            class="form-input"
            placeholder="Введите вес"
            min="0"
            step="0.5"
          />
        </div>

        <div class="form-group">
          <label
            class="form-label"
            for="edit-exercise-approaches"
          >Подходы</label>
          <input
            type="number"
            id="edit-exercise-approaches"
            class="form-input"
            placeholder="Введите количество подходов"
            min="1"
          />
        </div>

      </form>
    </div>

    <div class="modal-footer">
      <button class="cancel-btn" onclick="closeEditModal()">Отмена</button>
      <button class="save-btn" onclick="editTask()">Сохранить изменения</button>
    </div>
  </div>
</div>