<div class="session-header">
    <p class="session-label">{{t.workoutSession.labels.duration}}</p>
    <div class="timer-display">
        <div class="timer-unit">
            <span class="timer-value">00</span>
            <span class="timer-sub">{{t.workoutSession.labels.hours}}</span>
        </div>
        <span class="timer-separator">:</span>
        <div class="timer-unit">
            <span class="timer-value">00</span>
            <span class="timer-sub">{{t.workoutSession.labels.minutes}}</span>
        </div>
        <span class="timer-separator">:</span>
        <div class="timer-unit">
            <span class="timer-value">00</span>
            <span class="timer-sub">{{t.workoutSession.labels.seconds}}</span>
        </div>
    </div>
    <p class="session-info">
        <span id="current-date-display"></span> •
        <span id="remaining-exercises-display"></span>
    </p>
</div>