<nav class="bottom-nav">
  <div class="nav-item" onclick="navigateTo('trainMode')">
    <i class="fas fa-dumbbell"></i>
    <span>{{t.profile.common.nav_training}}</span>
  </div>
  <div class="nav-item" onclick="navigateTo('statistics')">
    <i class="fas fa-chart-bar"></i>
    <span>{{t.profile.common.nav_progress}}</span>
  </div>
  <div class="nav-item" onclick="navigateTo('trainingPlan')">
    <i class="fas fa-calendar-alt"></i>
    <span>{{t.profile.common.nav_plan}}</span>
  </div>
  <div class="nav-item" onclick="navigateTo('profileMain')">
    <i class="fas fa-user"></i>
    <span>{{t.profile.common.nav_profile}}</span>
  </div>
</nav>