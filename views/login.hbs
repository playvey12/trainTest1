<!DOCTYPE html>
<html lang="ru">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="./css/login.css" />
    <title>{{t.auth.login_title}}</title>
    {{> seo}}
</head>

<body>
    <div class="container">
        <header class="hero-section">
            <h1 class="main-title">{{t.auth.hero_title}}</h1>
            <p class="subtitle">{{t.auth.hero_subtitle}}</p>
        </header>

        <main class="main-content">
            <div class="registration-card">
                <div class="card-header">
                    <span class="step-text">{{t.auth.step_label}}</span>
                    <span class="step-count">{{t.auth.step_2_of_3}}</span>
                </div>
                <div class="progress-bar">
                    <div class="progress-fill" style="width: 66%;"></div>
                </div>

                <form id="registrationForm">
                    <div class="form-group">
                        <label class="form-label">{{t.auth.email_label}}</label>
                        <div class="input-wrapper">
                            <span class="input-icon"><i class="fa-regular fa-envelope"></i></span>
                            <input type="email" class="form-input" id="email" placeholder="{{t.auth.email_placeholder}}"
                                required>
                        </div>
                        <div class="error-message" id="emailError">{{t.auth.email_error}}</div>
                    </div>

                    <div class="form-group">
                        <label class="form-label">{{t.auth.password_label}}</label>
                        <div class="password-wrapper">
                            <span class="input-icon"><i class="fa-solid fa-lock"></i></span>
                            <input type="password" class="form-input" id="password" placeholder="••••••••••••" required>
                            <button type="button" class="toggle-password" id="togglePassword">
                                <i class="fa-regular fa-eye"></i>
                            </button>
                        </div>
                    </div>

                    <button type="submit" class="submit-btn" onclick="loginUser(event)" id="submitBtn">
                        <span id="btnText">{{t.auth.login_btn}}</span>

                    </button>

                    <div class="divider">
                        <span>{{t.auth.social_divider}}</span>
                    </div>

                    <div class="auth-footer">
                        <div class="footer-row">
                            <div class="login-link">
                                {{t.auth.already_have_acc}} <a href="/register"
                                    class="sign-in-link">{{t.auth.sign_in_link}}</a><a href="#" id="openResetModal"
                                    class="sign-in-link sign-in-link-forgot">
                                    •• {{t.auth.forgot_password}}?
                                </a>
                            </div>
                        </div>
                    </div>

            </div>
            <footer class="footer-info">
                {{t.auth.footer_terms}}
            </footer>
            </form>
    </div>

    </main>


    </div>

    {{>modalWindowForgot}}
    <script src="./js/register.js"></script>
    <script src="./js/auth.js"></script>
</body>

</html>